test_pattern:
  - 'none'
tests:
  - type: junit
    command: |
      set -o errexit -o pipefail # Exit on error
      cp no_file_attributes.xml service/rspec.xml
    report_files:
      - "service/*.xml"

